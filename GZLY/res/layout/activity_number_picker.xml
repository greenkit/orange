<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/drakwhite"
    android:gravity="center"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".NumberPickerActivity" >

    <com.standny.ui.itempicker.ItemPicker
        android:id="@+id/itemPicker"
        android:layout_height="wrap_content"
		android:layout_width="200dp" >

    </com.standny.ui.itempicker.ItemPicker>

    <Button
        android:id="@+id/setting_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/itemPicker"
        android:layout_below="@+id/itemPicker"
        android:layout_marginTop="17dp"
        android:paddingLeft="30dp"
        android:paddingRight="30dp"
        android:text="@string/btn_setting" />

    <Button
        android:id="@+id/cancel_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/setting_btn"
        android:layout_alignBottom="@+id/setting_btn"
        android:layout_alignRight="@+id/itemPicker"
        android:paddingLeft="30dp"
        android:paddingRight="30dp"
        android:text="@string/btn_cancel" />

</RelativeLayout>
